class Ex3

    def determine_palindrome(string)
        return true if string.length == 0
        
        if string.at(0).eql(string.at(string.length)
            if string.length > 2
                determine_palindrom(string.slice(1, string.length - 1)
            else
                return true
            end   
        else
            return false
        end
        
    end
end
